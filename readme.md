This web application uses the OpenWeatherMap API to fetch weather data and display it on the home page.
It has a search feature that let's you search for any city and get the weather for that city.
It uses the unsplash API to also set the background to an image with the search query of the city.

![Main Image](images/main.png?raw=true "Title")